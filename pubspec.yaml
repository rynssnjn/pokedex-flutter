name: pokedex_flutter
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.0.0
  cupertino_icons: ^1.0.2
  carousel_slider: ^4.0.0-nullsafety.0
  http_parser: ^4.0.0
  freezed_annotation: any
  freezed: any
  json_serializable: any
  json_annotation: any
  
  rsj_f:
    git: 
      url: https://github.com/rynssnjn/rsj_f.git
      ref: feature/null-safety

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: any
  get_it: ^6.1.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/translations/translations_en.strings